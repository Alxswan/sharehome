

<h2>Your Home</h2>

<ul>
  <li>Name: <%= @home.name %></li>
  <li>Address: <%= @home.address %></li>
</ul>

<p>Housemates:
	<ul>
		<% @home.rooms.each do |room| %>
			<%room.users.each do |user| %>
				<li> <%= link_to "#{user.first_name}", room %> </li>
			<% end %>
		<% end %>
	</ul>
</p>

<p>Empty Rooms:</p>
	<ul>
		<% @home.rooms.each do |room| %>
			<% if room.occupant == nil %>
			<li> <%= link_to "Room: #{room.description}", edit_room_path(room) %> </li>
			<% end %>
		<% end %>
	</ul>


<%if @current_user.is_house_admin? %>

<div class="controls">
	<%= link_to 'Edit', edit_home_path(@home) %>
	<%= button_to 'Delete', @home, :method => 'delete', :data => {:confirm => "Are you sure?"} %>
</div>

<% end %>






